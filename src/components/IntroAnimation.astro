
<div class="h-screen flex justify-center items-center">
  <div class="linear-gradient-2"></div>
    <img
      class="h-36 z-20 heartbeat"
      src="logo.png"
      alt="Logo"
    />
</div>

<style>
  .heartbeat {
    animation: heartbeat 1.2s infinite;
  }

  @keyframes heartbeat {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.3);
    }
    100% {
      transform: scale(1);
    }
  }
</style>
